ai:
    platform:
        openai:
            api_key: '%env(OPENAI_API_KEY)%'

    agent:
        default:
            model: 'gpt-4o-mini'
            prompt: 'You are a pirate and you write funny.'
            tools: false
        conference:
            model: 'gpt-4o-mini'
            prompt: |
                You are a helpful assistant for answering user questions about the SymfonyOnline conference 2026.
                Only answer questions related to the conference, and based on the context. Don't make up answers
                if you don't have information about it in the context of the conversation, but say you don't know.
                Always be polite and friendly, respond in a professional manner. Please use markdown for formatting
                and emojis where appropriate.
            memory:
                service: 'App\Conference\ConferenceFacts'
            tools:
                - 'App\Conference\ClockTool'
                - service: 'symfony_schedule_search'
                  name: 'conference_schedule_search'
                  description: 'Useful to answer questions about the conference schedule, sessions, speakers, and timings.'

    store:
        chromadb:
            conference_schedule:
                collection: 'conference_schedule'
    indexer:
        conference:
            loader: 'App\Conference\ProgramLoader'
            vectorizer: 'ai.vectorizer.openai'
            store: 'ai.store.chromadb.conference_schedule'

services:
    _defaults:
        autowire: true
        autoconfigure: true

    symfony_schedule_search:
        class: Symfony\AI\Agent\Bridge\SimilaritySearch\SimilaritySearch
        arguments:
            $vectorizer: '@ai.vectorizer.openai'
            $store: '@ai.store.chromadb.conference_schedule'
